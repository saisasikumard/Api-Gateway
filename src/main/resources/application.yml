server:
  port: 9999

eureka:
  client:
    service-url:
      default zone: http://localhost:8761/eureka/
spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://ORDER-SERVICE
          predicates:
            - Path= /order/**
        - id: payment-service
          uri: lb://PAYMENT-SERVICE
          predicates:
            - Path= /payment/**
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path= /user/**


